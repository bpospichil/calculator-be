meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: {{base_url}}/health
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });

  test("status should be ok", function() {
    expect(res.body.status).to.equal("ok");
  });
}
