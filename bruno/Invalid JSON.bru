meta {
  name: Invalid JSON
  type: http
  seq: 8
}

post {
  url: {{base_url}}/calculate
  body: text
}

headers {
  Content-Type: application/json
}

body:text {
  {not valid json
}

tests {
  test("should return 400", function() {
    expect(res.status).to.equal(400);
  });

  test("error should mention invalid JSON", function() {
    expect(res.body.error).to.contain("invalid JSON");
  });
}
