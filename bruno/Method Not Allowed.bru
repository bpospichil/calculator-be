meta {
  name: Method Not Allowed
  type: http
  seq: 9
}

get {
  url: {{base_url}}/calculate
}

tests {
  test("should return 405", function() {
    expect(res.status).to.equal(405);
  });

  test("error should mention POST", function() {
    expect(res.body.error).to.contain("POST");
  });
}
